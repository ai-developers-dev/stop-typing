.DS_Store

# Local Xcode/arch overrides (not referenced by the project)
wispr/xcconfig-override.xcconfig

# Claude Code local settings (user-specific permissions)
.claude/settings.local.json

# API keys (never commit)
wispr/wispr/Secrets.plist
